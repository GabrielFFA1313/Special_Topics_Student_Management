$kind: http-request
url: "{{base_url}}/auth/login"
method: POST
body:
  type: json
  content: "{\r

    \  \"email\": \"admin@test.com\",\r

    \  \"password\": \"password\"\r

    }"
scripts:
  - type: afterResponse
    code: "const data = pm.response.json();\r

      if (data.success && data.data.token) {\r

      \    pm.collectionVariables.set(\"token\", data.data.token);\r

      }"
    language: text/javascript
order: 1789525937957
